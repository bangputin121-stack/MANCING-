from telegram import Update
from telegram.ext import ContextTypes

# Baru setelah itu kode lo yang ini:
async def fishing_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    # ... isi kode mancing lo ...
